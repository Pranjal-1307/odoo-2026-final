# PeoplePay360 Backend Environment Configuration
PROJECT_NAME="PeoplePay360"
API_V1_STR="/api/v1"
SECRET_KEY="peoplepay360-super-secret-key-odoo-hackathon-2026"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database Configuration (SQLite by default, or PostgreSQL)
DATABASE_URL="sqlite:///./peoplepay360.db"
# DATABASE_URL="postgresql://user:password@localhost:5432/peoplepay360"

# CORS Configuration (comma-separated or JSON list)
BACKEND_CORS_ORIGINS='["http://localhost:5173", "http://localhost:3000", "*"]'

# Email / SMTP Configuration (Simulated / Production)
SMTP_HOST="localhost"
SMTP_PORT=1025
SMTP_USER=""
SMTP_PASSWORD=""
EMAILS_FROM_EMAIL="payroll@peoplepay360.com"
EMAILS_FROM_NAME="PeoplePay360 HR & Payroll"
